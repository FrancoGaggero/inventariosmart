## 1. Contrato compartido y modelo de datos

- [x] 1.1 Agregar en `packages/shared` `proveedores.ts`: esquemas de proveedor (create/patch/query), precios (`PreciosCreateSchema` sin productos repetidos, fecha no futura), vista previa (`ESTADOS_FILA_IMPORTACION`, `FilaVistaPreviaSchema`, `VistaPreviaSchema`), confirmación y resultado, etiquetas en español; ampliar `ProductoSchema` con `proveedorPrincipal` y `ProductoPatchSchema` con `proveedorPrincipalId`; tests unitarios. Listo cuando: `pnpm --filter @inventariosmart/shared test` pasa con CP-02.1d, CP-02.3 y la validación de `items`
- [x] 1.2 Modelos `Proveedor`, `PrecioProveedor`, enum `OrigenPrecio` y `Producto.proveedorPrincipalId` en `schema.prisma` (D1–D3); migración `20260923_suppliers_price_lists` con índices, `CHECK`s, RLS y privilegios de sólo inserción para `precio_proveedor`; agregar ambos modelos a `TENANT_MODELS`; `helpers.ts` limpia `precio_proveedor` y `proveedor` antes que `producto`; `rls.e2e-spec.ts` verifica privilegios de `precio_proveedor`. Listo cuando: `prisma migrate deploy` aplica en Neon dev, `rls.e2e-spec.ts` pasa con las tablas nuevas y CP-02.6b, y CP-02.4c (base) pasa
- [x] 1.3 Dependencias: `exceljs` en `apps/api` y `@types/multer` en dev; `pnpm install` con lockfile actualizado. Listo cuando: `pnpm --filter @inventariosmart/api typecheck` pasa con un `import('exceljs')` y `Express.Multer.File` tipado

## 2. API · módulo suppliers

- [x] 2.1 `SuppliersService`: `listar()` (búsqueda, `activo`, cursor `(nombre, id)`), `crear()` con nombre normalizado y 409, `obtener()`, `actualizar()` (reactivación incluida), `darDeBaja()`; `SuppliersController` con `@Roles('DUENIO')` y DTOs Swagger. Listo cuando: CP-02.1, CP-02.1b, CP-02.1c, CP-02.1d, CP-02.2, CP-02.3 y CP-02.7 (proveedores) pasan
- [x] 2.2 `PricesService.registrar()` (D4) con bloqueo de productos, regla RN-08 (principal automático, actualización del costo vigente, secundario sin efecto) y `listaVigente(proveedorId)` (`DISTINCT ON`); `POST /suppliers/:id/prices` y `GET /suppliers/:id/prices`. Listo cuando: CP-02.4, CP-02.5, CP-02.5b y CP-02.5d pasan
- [x] 2.3 Historial por producto `GET /products/:id/prices` (cursor `(vigenteDesde, id)` desc, DUENIO) y `ProductsService`: `LEFT JOIN proveedor` en listado y detalle (`proveedorPrincipal`), `PATCH` con `proveedorPrincipalId` (404 si es de otro comercio o inactivo → 409) que copia el último costo del proveedor, y `costoReposicion` manual que registra una fila `MANUAL` vía `PricesService` cuando hay principal. Listo cuando: CP-02.4b, CP-02.4c (rutas), CP-02.5c, CP-02.5e y CP-01.4d pasan y `products.e2e-spec.ts` sigue en verde
- [x] 2.4 `PriceListParser` (D5): `.xlsx` con `exceljs` diferido, `.csv` propio, detección de encabezados, decimales con coma y miles, repetidos, tope de 5.000 filas; unit tests con planillas generadas en el test. Listo cuando: los unit tests cubren CP-02.4g, repetidos, sin columnas reconocibles y tope de filas
- [x] 2.5 `POST /suppliers/:id/price-list/preview` (`FileInterceptor('archivo')`, 2 MB, memoria; clasificación `NUEVO|CAMBIA|IGUAL|SIN_PRODUCTO|INVALIDA` y resumen) y `POST /suppliers/:id/price-list` (lote, descarte de iguales, `PricesService` con `origen: IMPORT`); Swagger con `multipart/form-data`. Listo cuando: CP-02.4d, CP-02.4e y CP-02.4f pasan con un `.csv` y un `.xlsx` adjuntos desde el test
- [x] 2.6 Aislamiento y roles: CP-02.6 (proveedor y producto ajenos en carga manual, importación y `PATCH` de principal) y CP-02.7 (EMPLEADO y CONTADOR 403 en todas las rutas, incluido `GET /products/:id/prices`). Listo cuando: `suppliers.e2e-spec.ts` pasa completo junto con las suites existentes
- [x] 2.7 Regenerar contrato y cliente: `pnpm openapi`. Listo cuando: `docs/openapi.json` tiene las diez rutas de D6 con sus esquemas (incluido el multipart) y CI no reporta contrato desactualizado

## 3. Web · proveedores y costos

- [ ] 3.1 Cliente de datos `lib/proveedores.ts`: `useProveedores`, `useProveedor`, mutaciones crear/editar/baja/reactivar, `usePreciosProveedor`, `useCargarPrecios`, `useVistaPrevia` (multipart con `fetch` y el token, fuera de openapi-fetch si el cliente no soporta `FormData` tipado), `useConfirmarImportacion`, `useHistorialCostos(productoId)`; invalidación de proveedores y productos. Listo cuando: cargar o importar costos refresca la lista vigente y el costo del producto en Inventario sin recargar
- [ ] 3.2 Páginas `/proveedores` (listado, buscador, chips, estrellas de confiabilidad, baja/reactivar) y `ProveedorForm` en `/proveedores/nuevo` y `/proveedores/:id/editar` (validación con `shared`, errores por campo, 409 junto al nombre). Listo cuando: un DUENIO crea, edita, da de baja y reactiva un proveedor desde la web y el nombre repetido muestra el mensaje del 409
- [ ] 3.3 Ficha `/proveedores/:id`: datos, lista vigente paginada, "Cargar costo" (buscador de producto + costo) e importación en tres pasos en `/proveedores/:id/importar` (archivo → vista previa con filtros por estado y resumen → confirmar → resultado). Listo cuando: una planilla `.csv` de prueba muestra filas en los cinco estados y, tras confirmar, la lista vigente y el costo del producto en Inventario reflejan los costos nuevos
- [ ] 3.4 Producto: selector de proveedor principal en `ProductoFormPage` (activos, "Sin proveedor", proveedor inactivo atenuado) e "Historial de costos" al editar; enlace "Proveedores" en `AppShell` y tarjeta en el inicio sólo DUENIO; `RequireRole(['DUENIO'])`. Listo cuando: cambiar el proveedor principal actualiza el costo mostrado, EMPLEADO no ve "Proveedores" y, si escribe la URL, ve "No tenés permiso"

## 4. Documentación y cierre

- [x] 4.1 ADR `docs/adr/0007-listas-de-precios-historial-y-costo-vigente.md` (D10); README (rutas de HU-02, formato de planilla), `docs/arquitectura.html` (§5 DER: `Proveedor` y `PrecioProveedor` con las columnas de D1/D2; §6 endpoints de `suppliers`), runbook `rls.md` (segunda tabla de sólo inserción), `openspec/CAPACIDADES.md`; plantilla `docs/plantillas/lista-de-precios.csv` enlazada desde la web. Listo cuando: los documentos reflejan D1 a D6
- [x] 4.2 `pnpm lint`, `pnpm format:check`, `pnpm typecheck`, `pnpm test`, e2e de la API en verde local; push y CI en verde. Listo cuando: el run de CI del commit final tiene los dos jobs en verde
- [ ] 4.3 Desplegar y verificar en producción: migración aplicada en Neon production, crear un proveedor real, importar una planilla `.csv` chica desde la web publicada y comprobar el costo nuevo del producto en Inventario. Listo cuando: el proveedor y su lista aparecen en `https://inventariosmart0.vercel.app/proveedores` y el costo del producto cambió
- [ ] 4.4 (manual, Franco) Mover HU-02 a Hecho en Trello, actualizar `Backlog_InventarioSmart_v2.xlsx` y la tarea correspondiente del Gantt. Listo cuando: Trello, backlog y Gantt coinciden
