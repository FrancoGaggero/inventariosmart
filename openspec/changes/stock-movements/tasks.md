## 1. Contrato compartido y modelo de datos

- [x] 1.1 Agregar en `packages/shared` `movimientos.ts`: `TIPOS_MOVIMIENTO`, `MOTIVOS_MOVIMIENTO`, etiquetas en español, `MovimientoSchema`, `MovimientoCreateSchema` (discriminado por tipo: cantidad > 0 en VENTA/INGRESO, entero ≠ 0 y motivo obligatorio en AJUSTE, fecha ISO no futura con 5 min de tolerancia, observación ≤ 200), `AnulacionSchema`, `MovimientosQuerySchema` (`hasta >= desde`) y `ListaMovimientosSchema`, con tests unitarios. Listo cuando: `pnpm --filter @inventariosmart/shared test` pasa con los casos de CP-10.1c, CP-10.1d, CP-10.1e y CP-10.5c
- [x] 1.2 Modelo `Movimiento` y enums en `schema.prisma` (D1), relación con `Producto` y `Usuario`; migración `20260922_stock_movements` con índices, `CHECK`s (incluido `producto.stock_actual >= 0`), RLS (`movimiento_tenant`/`movimiento_sistema`, `ENABLE`+`FORCE`), privilegios de D2 (`REVOKE UPDATE, DELETE`, `GRANT UPDATE (anulado_por_id)`) y backfill de `STOCK_INICIAL` (D6); agregar `Movimiento` a `TENANT_MODELS` y limpiar `movimiento` antes que `producto` en `test/helpers.ts`. Listo cuando: `prisma migrate deploy` aplica en Neon dev, `rls.e2e-spec.ts` pasa incluyendo `movimiento` y CP-10.7c, y `psql` como `app_api` sin contexto devuelve 0 filas de `movimiento` (CP-10.7b)

## 2. API · módulo movements

- [x] 2.1 `MovementsService.registrar()` y `registrarEnTransaccion()` (D3): bloqueo del producto, 404/409 por producto ajeno o dado de baja, cálculo de `efectoStock` y `stockResultante`, 409 `CONFLICTO` con `details { stockActual, cantidad }` si queda negativo, `precioUnitario` en VENTA, `UPDATE producto`, respuesta con `estadoStock`. Listo cuando: CP-10.1, CP-10.1b, CP-10.1c, CP-10.2, CP-10.2b (10 ventas en paralelo), CP-10.3, CP-10.3b, CP-10.3c y CP-10.10 pasan
- [x] 2.2 Idempotencia (D5): lectura por `(comercio, clave)`, reintento ante P2002, 200 con el movimiento existente. Listo cuando: CP-10.8 y CP-10.8b pasan
- [x] 2.3 `anular()` (D4): AJUSTE inverso con `motivo: ANULACION` y `corrigeAId`, `anuladoPorId` en el original, 409 si ya anulado o si es anulación, 404 si es de otro comercio. Listo cuando: CP-10.4, CP-10.4b y CP-10.7 pasan
- [x] 2.4 `listar()` y `obtener()` (D7): SQL con `JOIN producto` y `JOIN usuario`, filtros `productoId`, `tipo`, `desde`, `hasta`, cursor `(fecha, id)`. Listo cuando: CP-10.5, CP-10.5b y CP-10.5c pasan; unit test del cursor
- [x] 2.5 `MovementsController` con `@Roles('DUENIO','EMPLEADO','CONTADOR')` en lectura, `@Roles('DUENIO','EMPLEADO')` en `POST /movements`, `@Roles('DUENIO')` en `POST /movements/:id/anular`, código 201/200 según `creado`, DTOs Swagger (`MovimientoDto`, `MovimientoCreateBodyDto`, `AnulacionBodyDto`, `ListaMovimientosDto`), `@ApiHeader Idempotency-Key`, respuestas 400/401/403/404/409. Listo cuando: CP-10.4c (sin PATCH/DELETE), CP-10.4d, CP-10.6 (EMPLEADO sin `costo*`) y CP-10.6b (CONTADOR 200 en lectura, 403 en escritura) pasan
- [x] 2.6 `ProductsService.crear()` registra el `INGRESO STOCK_INICIAL` en la misma transacción cuando `stockInicial > 0` (D6); `ProductsModule` importa `MovementsModule`. Listo cuando: CP-10.9 pasa, el backfill de 1.2 cubre CP-10.9b con un producto creado por SQL antes de la migración en el test, y `products.e2e-spec.ts` sigue en verde
- [x] 2.7 Regenerar contrato y cliente: `pnpm openapi`. Listo cuando: `docs/openapi.json` tiene las cuatro rutas de D8 con sus esquemas y CI no reporta contrato desactualizado

## 3. Web · movimientos

- [ ] 3.1 Cliente de datos `lib/movimientos.ts`: `useMovimientos(filtros)` con `useInfiniteQuery`, `useMovimiento(id)`, `useRegistrarMovimiento()` (genera y renueva la `Idempotency-Key`, invalida movimientos y productos) y `useAnularMovimiento()`. Listo cuando: el historial carga páginas sucesivas con "Ver más" y registrar o anular refresca el stock en Inventario sin recargar
- [ ] 3.2 Página `/movimientos`: filtros tipo, desde/hasta y producto (chip con el nombre cuando llega `?productoId=`), tabla (fecha, tipo, producto, cantidad con signo, stock resultante, usuario, motivo/observación), fila atenuada con "Anulado" y botón "Anular" con confirmación sólo para DUENIO; botón "Registrar movimiento" para DUENIO y EMPLEADO. Listo cuando: CONTADOR ve el historial sin botones de escritura y un DUENIO anula desde la fila y ve el ajuste inverso arriba
- [ ] 3.3 Formulario `/movimientos/nuevo?productoId=&tipo=`: segmentos Venta/Ingreso/Ajuste, buscador de producto (reutiliza `useProductos({ q })`) con stock actual visible, cantidad, fecha y hora, motivo según tipo, observación; validación con `MovimientoCreateSchema`, errores por campo desde `details`, 409 de stock insuficiente mostrado junto a cantidad; al confirmar `Aviso` con stock resultante y tono de advertencia si `estadoStock` no es `OK`; "Registrar otro". Listo cuando: una venta mayor al stock muestra el mensaje del 409 sin salir del formulario y una venta que deja stock bajo muestra el aviso
- [ ] 3.4 Accesos: enlace "Movimientos" en `AppShell` para los tres roles, acciones "Vender/Ingresar" e "Historial" por fila en `/productos` (DUENIO y EMPLEADO), tarjeta "Movimientos" y resumen de stock bajo/sin stock en el inicio (D10), `RequireRole` en `/movimientos/nuevo`. Listo cuando: CONTADOR no ve "Registrar" y, si escribe `/movimientos/nuevo`, ve "No tenés permiso"; el inicio muestra el conteo correcto tras una venta que deja stock bajo

## 4. Documentación y cierre

- [x] 4.1 ADR `docs/adr/0006-movimientos-inmutables-stock-transaccional.md` (D12); actualizar README (rutas nuevas, cabecera `Idempotency-Key`), `docs/arquitectura.html` (§5 DER: `Movimiento` con las columnas de D1; §6 endpoints: `POST /movements/:id/anular` en lugar de `/ajuste`), runbook `rls.md` (nota sobre tablas de sólo inserción) y `openspec/CAPACIDADES.md`. Listo cuando: los documentos reflejan D1 a D8
- [ ] 4.2 `pnpm lint`, `pnpm format:check`, `pnpm typecheck`, `pnpm test`, e2e de la API en verde local; push y CI en verde. Listo cuando: el run de CI del commit final tiene los dos jobs en verde
- [ ] 4.3 Desplegar y verificar en producción: migración aplicada en Neon production (backfill incluido), registrar una venta real desde la web publicada con el usuario de Franco, verla en el historial y comprobar el stock en Inventario. Listo cuando: el movimiento aparece en `https://inventariosmart0.vercel.app/movimientos` y el stock del producto cambió
- [ ] 4.4 (manual, Franco) Mover HU-10 a Hecho en Trello, actualizar `Backlog_InventarioSmart_v2.xlsx` y la tarea correspondiente del Gantt. Listo cuando: Trello, backlog y Gantt coinciden
